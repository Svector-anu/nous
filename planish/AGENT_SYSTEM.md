Personality = short system prompt + optional embedding.
Memory: episodic (recent events) + summary (compressed by LLM occasionally).
Needs: energy, hunger, social, safety (drive FSM).
Roles emerge (builder, fighter, trader, leader) — do not hard-code too early.
Clan = shared blackboard section + leadership election / claim.
Disasters & chaos events write directly to blackboard and trigger local FSM reactions + higher-level LLM if needed.
